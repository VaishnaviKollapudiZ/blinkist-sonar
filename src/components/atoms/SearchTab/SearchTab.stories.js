import React from "react";
import SearchTab from "./SearchTab";
export default {
	title: "atoms/SearchBar",
};

const Template = (args) => <SearchTab {...args} />;
export const Primary = Template.bind({});
